<?xml version="1.0" encoding="utf-8"?>


<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginLeft="8dp"
    android:layout_marginRight="4dp"
    android:layout_marginBottom="@dimen/activity_vertical_margin"
    android:elevation="0dp">


    <ImageView
        android:id="@+id/nsImageView"
        android:layout_width="198dp"
        android:layout_height="305dp"
        android:layout_marginBottom="8dp"
        android:contentDescription="@string/poster"
        app:layout_constraintBottom_toTopOf="@+id/nsTvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        app:srcCompat="@drawable/poster_johnwick" />

    <TextView
        android:id="@+id/nsTvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/john_wick_chapter_3_parabellum"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        android:typeface="sans"
        app:layout_constraintEnd_toEndOf="@+id/nsImageView"
        app:layout_constraintStart_toStartOf="@+id/nsImageView"
        app:layout_constraintTop_toBottomOf="@+id/nsImageView" />

    <RatingBar
        android:id="@+id/nsRatingBar"
        style="@style/Widget.AppCompat.RatingBar.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:rating="4"
        android:theme="@style/RatingBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/nsTvTitle"

        />

</androidx.constraintlayout.widget.ConstraintLayout>

